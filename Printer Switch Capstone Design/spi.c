#include <avr/io.h>
#include <stdlib.h>



/*
void SPI_masterTransmit(uint8_t data)
{

    // load data into register
    SPDR = data;

    // Wait for transmission complete
    while(!(SPSR & (1 << SPIF)));
}
*/
